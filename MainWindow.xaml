<Window x:Class="AppHiderNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppHiderNet"
        mc:Ignorable="d"
        Title="App Hider Manager" Height="400" Width="500"
        Icon="app_icon.png"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" GlassFrameThickness="-1"/>
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource GlassBackgroundBrush}" 
            BorderBrush="{StaticResource GlassBorderBrush}" 
            BorderThickness="1" 
            CornerRadius="16"
            MouseLeftButtonDown="Window_MouseLeftButtonDown">
        
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Optional: Add an icon here if available, or just text -->
                    <TextBlock Text="App Hider" 
                               Foreground="{StaticResource GlassTextBrush}" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Manager" 
                               Foreground="{StaticResource GlassTextSecondaryBrush}" 
                               FontSize="20" 
                               FontWeight="Regular" 
                               VerticalAlignment="Center"
                               Margin="5,0,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1"
                        Content="✕" 
                        Width="32" Height="32"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource GlassTextSecondaryBrush}"
                        FontSize="16"
                        Click="CloseButton_Click"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="Transparent" CornerRadius="16">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#20FFFFFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#40FF4444"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Main Content -->
            <ListBox x:Name="HiddenAppsList" Grid.Row="1" Margin="0,0,0,20" DisplayMemberPath="Value" SelectedValuePath="Key"/>

            <!-- Action Buttons -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Content="Scan Apps" Width="100" Height="35" Click="ScanApps_Click" HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="2">
                    <Button Content="Restore" Width="100" Height="35" Click="RestoreSelected_Click"/>
                    <Button Content="Restore All" Width="100" Height="35" Click="RestoreAll_Click" Margin="10,0"/>
                    <Button Content="Kill" Width="80" Height="35"
                            Background="#20FF4444" 
                            BorderBrush="#40FF4444" 
                            Click="KillSelected_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
