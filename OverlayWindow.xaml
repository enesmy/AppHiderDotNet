<Window x:Class="AppHiderNet.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay" 
        Height="20" Width="30"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Focusable="False">
    <Grid>
        <Button Name="HideButton" 
                Width="30" Height="30" 
                Click="HideButton_Click"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" CornerRadius="15" BorderThickness="1" BorderBrush="#60FFFFFF">
                        <Border.Background>
                            <RadialGradientBrush GradientOrigin="0.3,0.3">
                                <GradientStop Color="#800078D7" Offset="0"/>
                                <GradientStop Color="#CC005A9E" Offset="1"/>
                            </RadialGradientBrush>
                        </Border.Background>
                        <Path Data="M 0 5 L 5 10 L 10 5" 
                              Stroke="White" 
                              StrokeThickness="2" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <RadialGradientBrush GradientOrigin="0.3,0.3">
                                        <GradientStop Color="#A00099FF" Offset="0"/>
                                        <GradientStop Color="#E00078D7" Offset="1"/>
                                    </RadialGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
